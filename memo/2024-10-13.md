---
title: 描画性能を自動化する
tags: [Astro]
---

三通り試して最後の形に落ち着いた。

## 背景

この方式は規模が大きくなると破綻する。同じ症状で検索しても出てこなかった。

## やったこと

```tsx
export function Badge({ label }: Props) {
  return <span class="badge">{label}</span>;
}
```

手元では再現するのに CI では通る、という状態だった。

## わかったこと

結局は基本に忠実なやり方が一番速かった。
