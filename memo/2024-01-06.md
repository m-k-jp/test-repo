---
title: 文字コードを自動化する
tags: [運用, Astro, フロントエンド]
---

三通り試して最後の形に落ち着いた。

## 背景

手元では再現するのに CI では通る、という状態だった。代替案も検討したが、運用コストが見合わなかった。

## やったこと

```yaml
- name: Cache
  uses: actions/cache@v4
  with:
    path: ~/.npm
    key: ${{ runner.os }}-npm
```

数字で見ると差は明らかだった。

## わかったこと

記録しておかないとまた忘れる。
