---
title: OAuthの落とし穴
tags: [CI]
---

二度目なので今度こそ記録に残す。

## 背景

手元では再現するのに CI では通る、という状態だった。設定を一つ変えるだけで挙動が変わる。

## やったこと

```ts
export function toSlug(input: string): string {
  return input.trim().toLowerCase().replace(/\s+/g, '-');
}
```

ドキュメントには書かれているが、条件が一つある。

## わかったこと

しばらくこの形で運用してみる。
