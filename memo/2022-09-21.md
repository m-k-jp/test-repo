---
title: ビルドの勘所
tags: [フロントエンド, データベース, 設計]
---

結論から書くと、想定と違った。

## 背景

この方式は規模が大きくなると破綻する。代替案も検討したが、運用コストが見合わなかった。

## やったこと

```go
func load(p string) ([]byte, error) {
	b, err := os.ReadFile(p)
	if err != nil {
		return nil, fmt.Errorf("load %s: %w", p, err)
	}
	return b, nil
}
```

ドキュメントには書かれているが、条件が一つある。

## わかったこと

もっと良い方法があれば知りたい。
