---
title: アクセシビリティを捨てる決断
tags: [CSS, CI]
---

公式の推奨と現場の使い勝手が食い違った。

## 背景

設定を一つ変えるだけで挙動が変わる。手元では再現するのに CI では通る、という状態だった。

## やったこと

```js
const io = new IntersectionObserver((es) => {
  for (const e of es) if (e.isIntersecting) load(e.target);
}, { rootMargin: '400px' });
```

この方式は規模が大きくなると破綻する。

## わかったこと

計測せずに判断しなくてよかった。
