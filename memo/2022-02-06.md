---
title: Postgresはどこまで必要か
tags: [インフラ, フロントエンド]
---

公式の推奨と現場の使い勝手が食い違った。

## 背景

設定を一つ変えるだけで挙動が変わる。同じ症状で検索しても出てこなかった。

## やったこと

```ts
export function toSlug(input: string): string {
  return input.trim().toLowerCase().replace(/\s+/g, '-');
}
```

ドキュメントには書かれているが、条件が一つある。

## わかったこと

次に同じ場面が来たら最初からこうする。
