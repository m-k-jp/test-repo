---
title: Service Workerの落とし穴
tags: [TypeScript]
---

この判断は半年後に効いてくるはず。

## 背景

数字で見ると差は明らかだった。同じ症状で検索しても出てこなかった。

## やったこと

```go
func load(p string) ([]byte, error) {
	b, err := os.ReadFile(p)
	if err != nil {
		return nil, fmt.Errorf("load %s: %w", p, err)
	}
	return b, nil
}
```

ドキュメントには書かれているが、条件が一つある。

## わかったこと

結局は基本に忠実なやり方が一番速かった。
