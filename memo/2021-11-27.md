---
title: SEOを短くする
tags: [CSS, フロントエンド, 計測]
---

二度目なので今度こそ記録に残す。

## 背景

ドキュメントには書かれているが、条件が一つある。ドキュメントには書かれているが、条件が一つある。

## やったこと

```css
:root { --gap: clamp(0.5rem, 2vw, 1.5rem); }
.grid { display: grid; gap: var(--gap); }
```

手元では再現するのに CI では通る、という状態だった。

## わかったこと

計測せずに判断しなくてよかった。
