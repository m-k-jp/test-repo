---
title: 正規表現を整理する
tags: [運用, TypeScript]
---

三通り試して最後の形に落ち着いた。

## 背景

手元では再現するのに CI では通る、という状態だった。同じ症状で検索しても出てこなかった。

## やったこと

```tsx
export function Badge({ label }: Props) {
  return <span class="badge">{label}</span>;
}
```

代替案も検討したが、運用コストが見合わなかった。

## わかったこと

もっと良い方法があれば知りたい。
