---
title: WebAssemblyはどこまで必要か
tags: [CSS, データベース, Astro]
---

三通り試して最後の形に落ち着いた。

## 背景

原因は思っていたところとは別にあった。原因は思っていたところとは別にあった。

## やったこと

```rust
fn parse(s: &str) -> Result<Config, Error> {
    let c: Config = toml::from_str(s)?;
    c.validate()?;
    Ok(c)
}
```

原因は思っていたところとは別にあった。

## わかったこと

計測せずに判断しなくてよかった。
