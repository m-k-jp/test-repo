---
title: テストで詰まった話
tags: [フロントエンド, CI, TypeScript]
---

先週から気になっていたので調べた。

## 背景

設定を一つ変えるだけで挙動が変わる。ドキュメントには書かれているが、条件が一つある。

## やったこと

```python
def chunk(items, size):
    for i in range(0, len(items), size):
        yield items[i:i + size]
```

設定を一つ変えるだけで挙動が変わる。

## わかったこと

しばらくこの形で運用してみる。
