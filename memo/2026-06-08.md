---
title: ログ設計の使い分け
tags: [インフラ, 設計, フロントエンド]
---

小さな話だが積み重なると効いてくる。

## 背景

同じ症状で検索しても出てこなかった。手元では再現するのに CI では通る、という状態だった。

## やったこと

```go
func load(p string) ([]byte, error) {
	b, err := os.ReadFile(p)
	if err != nil {
		return nil, fmt.Errorf("load %s: %w", p, err)
	}
	return b, nil
}
```

ドキュメントには書かれているが、条件が一つある。

## わかったこと

記録しておかないとまた忘れる。
