---
title: バックアップの限界を測る
tags: [CI, フロントエンド]
---

二度目なので今度こそ記録に残す。

## 背景

ドキュメントには書かれているが、条件が一つある。設定を一つ変えるだけで挙動が変わる。

## やったこと

```yaml
- name: Cache
  uses: actions/cache@v4
  with:
    path: ~/.npm
    key: ${{ runner.os }}-npm
```

代替案も検討したが、運用コストが見合わなかった。

## わかったこと

次に同じ場面が来たら最初からこうする。
