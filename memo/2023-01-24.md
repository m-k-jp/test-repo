---
title: テストを計測してみた
tags: [設計, 運用, CSS]
---

三通り試して最後の形に落ち着いた。

## 背景

手元では再現するのに CI では通る、という状態だった。この方式は規模が大きくなると破綻する。

## やったこと

```sh
#!/bin/sh
set -eu
find dist -name '*.js' -exec gzip -kf {} +
du -sh dist | cut -f1
```

数字で見ると差は明らかだった。

## わかったこと

もっと良い方法があれば知りたい。
