---
title: SEOで失敗した
tags: [インフラ, CI]
---

うまくいかなかった記録も残しておく。

## 背景

同じ症状で検索しても出てこなかった。設定を一つ変えるだけで挙動が変わる。

## やったこと

```rust
fn parse(s: &str) -> Result<Config, Error> {
    let c: Config = toml::from_str(s)?;
    c.validate()?;
    Ok(c)
}
```

この方式は規模が大きくなると破綻する。

## わかったこと

結局は基本に忠実なやり方が一番速かった。
