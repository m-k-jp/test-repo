---
title: 命名を整理する
tags: [CSS, Astro, データベース]
---

最初は複雑にしすぎていた。削ったら短くなった。

## 背景

同じ症状で検索しても出てこなかった。設定を一つ変えるだけで挙動が変わる。

## やったこと

```tsx
export function Badge({ label }: Props) {
  return <span class="badge">{label}</span>;
}
```

数字で見ると差は明らかだった。

## わかったこと

記録しておかないとまた忘れる。
