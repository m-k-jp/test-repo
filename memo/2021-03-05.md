---
title: キャッシュで失敗した
tags: [設計, データベース]
---

三通り試して最後の形に落ち着いた。

## 背景

設定を一つ変えるだけで挙動が変わる。ドキュメントには書かれているが、条件が一つある。

## やったこと

```yaml
- name: Cache
  uses: actions/cache@v4
  with:
    path: ~/.npm
    key: ${{ runner.os }}-npm
```

この方式は規模が大きくなると破綻する。

## わかったこと

次に同じ場面が来たら最初からこうする。
