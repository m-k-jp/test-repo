---
title: 正規表現の運用メモ
tags: [データベース]
---

先週から気になっていたので調べた。

## 背景

手元では再現するのに CI では通る、という状態だった。ドキュメントには書かれているが、条件が一つある。

## やったこと

```sh
#!/bin/sh
set -eu
find dist -name '*.js' -exec gzip -kf {} +
du -sh dist | cut -f1
```

設定を一つ変えるだけで挙動が変わる。

## わかったこと

記録しておかないとまた忘れる。
