---
title: 文字コードで失敗した
tags: [インフラ, TypeScript]
---

三通り試して最後の形に落ち着いた。

## 背景

原因は思っていたところとは別にあった。代替案も検討したが、運用コストが見合わなかった。

## やったこと

```sql
CREATE INDEX idx_meta ON documents USING gin (meta jsonb_path_ops);
EXPLAIN ANALYZE SELECT id FROM documents WHERE meta @> '{"k":1}';
```

この方式は規模が大きくなると破綻する。

## わかったこと

計測せずに判断しなくてよかった。
