---
title: キャッシュの基本に立ち返る
tags: [運用, フロントエンド, CSS]
---

小さな話だが積み重なると効いてくる。

## 背景

原因は思っていたところとは別にあった。ドキュメントには書かれているが、条件が一つある。

## やったこと

```rust
fn parse(s: &str) -> Result<Config, Error> {
    let c: Config = toml::from_str(s)?;
    c.validate()?;
    Ok(c)
}
```

設定を一つ変えるだけで挙動が変わる。

## わかったこと

結局は基本に忠実なやり方が一番速かった。
