---
title: 描画性能の限界を測る
tags: [フロントエンド, 運用, インフラ]
---

この判断は半年後に効いてくるはず。

## 背景

同じ症状で検索しても出てこなかった。代替案も検討したが、運用コストが見合わなかった。

## やったこと

```rust
fn parse(s: &str) -> Result<Config, Error> {
    let c: Config = toml::from_str(s)?;
    c.validate()?;
    Ok(c)
}
```

同じ症状で検索しても出てこなかった。

## わかったこと

計測せずに判断しなくてよかった。
