---
title: Service Workerの設計指針
tags: [運用, インフラ]
---

この判断は半年後に効いてくるはず。

## 背景

この方式は規模が大きくなると破綻する。数字で見ると差は明らかだった。

## やったこと

```css
:root { --gap: clamp(0.5rem, 2vw, 1.5rem); }
.grid { display: grid; gap: var(--gap); }
```

同じ症状で検索しても出てこなかった。

## わかったこと

計測せずに判断しなくてよかった。
