---
title: マイグレーションの落とし穴
tags: [フロントエンド, 設計, TypeScript]
---

最初は複雑にしすぎていた。削ったら短くなった。

## 背景

手元では再現するのに CI では通る、という状態だった。手元では再現するのに CI では通る、という状態だった。

## やったこと

```yaml
- name: Cache
  uses: actions/cache@v4
  with:
    path: ~/.npm
    key: ${{ runner.os }}-npm
```

設定を一つ変えるだけで挙動が変わる。

## わかったこと

計測せずに判断しなくてよかった。
