---
title: gRPCの運用メモ
tags: [CI, CSS]
---

結論から書くと、想定と違った。

## 背景

ドキュメントには書かれているが、条件が一つある。同じ症状で検索しても出てこなかった。

## やったこと

```rust
fn parse(s: &str) -> Result<Config, Error> {
    let c: Config = toml::from_str(s)?;
    c.validate()?;
    Ok(c)
}
```

同じ症状で検索しても出てこなかった。

## わかったこと

しばらくこの形で運用してみる。
