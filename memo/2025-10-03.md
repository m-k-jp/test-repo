---
title: 命名を見直した
tags: [インフラ, フロントエンド, TypeScript]
---

二度目なので今度こそ記録に残す。

## 背景

同じ症状で検索しても出てこなかった。ドキュメントには書かれているが、条件が一つある。

## やったこと

```rust
fn parse(s: &str) -> Result<Config, Error> {
    let c: Config = toml::from_str(s)?;
    c.validate()?;
    Ok(c)
}
```

原因は思っていたところとは別にあった。

## わかったこと

もっと良い方法があれば知りたい。
