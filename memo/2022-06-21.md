---
title: タイムゾーンの運用メモ
tags: [Astro]
---

計測してみたら思い込みが崩れた。

## 背景

数字で見ると差は明らかだった。数字で見ると差は明らかだった。

## やったこと

```sql
CREATE INDEX idx_meta ON documents USING gin (meta jsonb_path_ops);
EXPLAIN ANALYZE SELECT id FROM documents WHERE meta @> '{"k":1}';
```

手元では再現するのに CI では通る、という状態だった。

## わかったこと

しばらくこの形で運用してみる。
