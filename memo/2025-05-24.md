---
title: タイムゾーンで詰まった話
tags: [TypeScript]
---

この判断は半年後に効いてくるはず。

## 背景

原因は思っていたところとは別にあった。設定を一つ変えるだけで挙動が変わる。

## やったこと

```tsx
export function Badge({ label }: Props) {
  return <span class="badge">{label}</span>;
}
```

手元では再現するのに CI では通る、という状態だった。

## わかったこと

もっと良い方法があれば知りたい。
