---
title: セマンティックバージョニングを捨てる決断
tags: [TypeScript, 運用, データベース]
---

この判断は半年後に効いてくるはず。

## 背景

数字で見ると差は明らかだった。設定を一つ変えるだけで挙動が変わる。

## やったこと

```tsx
export function Badge({ label }: Props) {
  return <span class="badge">{label}</span>;
}
```

原因は思っていたところとは別にあった。

## わかったこと

次に同じ場面が来たら最初からこうする。
