---
title: CSSを短くする
tags: [TypeScript, 設計, インフラ]
---

計測してみたら思い込みが崩れた。

## 背景

数字で見ると差は明らかだった。手元では再現するのに CI では通る、という状態だった。

## やったこと

```yaml
- name: Cache
  uses: actions/cache@v4
  with:
    path: ~/.npm
    key: ${{ runner.os }}-npm
```

原因は思っていたところとは別にあった。

## わかったこと

しばらくこの形で運用してみる。
