---
title: 監視を見直した
tags: [CSS]
---

この判断は半年後に効いてくるはず。

## 背景

数字で見ると差は明らかだった。手元では再現するのに CI では通る、という状態だった。

## やったこと

```tsx
export function Badge({ label }: Props) {
  return <span class="badge">{label}</span>;
}
```

同じ症状で検索しても出てこなかった。

## わかったこと

計測せずに判断しなくてよかった。
