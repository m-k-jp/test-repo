---
title: TypeScriptを計測してみた
tags: [フロントエンド]
---

先週から気になっていたので調べた。

## 背景

この方式は規模が大きくなると破綻する。この方式は規模が大きくなると破綻する。

## やったこと

```js
const io = new IntersectionObserver((es) => {
  for (const e of es) if (e.isIntersecting) load(e.target);
}, { rootMargin: '400px' });
```

この方式は規模が大きくなると破綻する。

## わかったこと

記録しておかないとまた忘れる。
