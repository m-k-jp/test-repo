---
title: タイムゾーンの設計指針
tags: [TypeScript, フロントエンド, 運用]
---

二度目なので今度こそ記録に残す。

## 背景

原因は思っていたところとは別にあった。数字で見ると差は明らかだった。

## やったこと

```rust
fn parse(s: &str) -> Result<Config, Error> {
    let c: Config = toml::from_str(s)?;
    c.validate()?;
    Ok(c)
}
```

数字で見ると差は明らかだった。

## わかったこと

結局は基本に忠実なやり方が一番速かった。
