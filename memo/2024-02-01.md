---
title: Nginxの落とし穴
tags: [TypeScript]
---

先週から気になっていたので調べた。

## 背景

この方式は規模が大きくなると破綻する。数字で見ると差は明らかだった。

## やったこと

```sh
#!/bin/sh
set -eu
find dist -name '*.js' -exec gzip -kf {} +
du -sh dist | cut -f1
```

手元では再現するのに CI では通る、という状態だった。

## わかったこと

次に同じ場面が来たら最初からこうする。
