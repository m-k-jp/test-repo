---
title: Postgresを見直した
tags: [設計, TypeScript]
---

小さな話だが積み重なると効いてくる。

## 背景

原因は思っていたところとは別にあった。手元では再現するのに CI では通る、という状態だった。

## やったこと

```sh
#!/bin/sh
set -eu
find dist -name '*.js' -exec gzip -kf {} +
du -sh dist | cut -f1
```

設定を一つ変えるだけで挙動が変わる。

## わかったこと

もっと良い方法があれば知りたい。
