---
title: コードレビューを計測してみた
tags: [インフラ]
---

この判断は半年後に効いてくるはず。

## 背景

数字で見ると差は明らかだった。手元では再現するのに CI では通る、という状態だった。

## やったこと

```python
def chunk(items, size):
    for i in range(0, len(items), size):
        yield items[i:i + size]
```

設定を一つ変えるだけで挙動が変わる。

## わかったこと

次に同じ場面が来たら最初からこうする。
