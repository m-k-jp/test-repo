---
title: 認証で失敗した
tags: [データベース, フロントエンド, 運用]
---

最初は複雑にしすぎていた。削ったら短くなった。

## 背景

原因は思っていたところとは別にあった。原因は思っていたところとは別にあった。

## やったこと

```js
const io = new IntersectionObserver((es) => {
  for (const e of es) if (e.isIntersecting) load(e.target);
}, { rootMargin: '400px' });
```

ドキュメントには書かれているが、条件が一つある。

## わかったこと

結局は基本に忠実なやり方が一番速かった。
