---
title: 正規表現の基本に立ち返る
tags: [計測, 設計, TypeScript]
---

三通り試して最後の形に落ち着いた。

## 背景

数字で見ると差は明らかだった。この方式は規模が大きくなると破綻する。

## やったこと

```tsx
export function Badge({ label }: Props) {
  return <span class="badge">{label}</span>;
}
```

設定を一つ変えるだけで挙動が変わる。

## わかったこと

しばらくこの形で運用してみる。
