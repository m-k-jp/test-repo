---
title: ビルドの落とし穴
tags: [計測]
---

二度目なので今度こそ記録に残す。

## 背景

ドキュメントには書かれているが、条件が一つある。手元では再現するのに CI では通る、という状態だった。

## やったこと

```yaml
- name: Cache
  uses: actions/cache@v4
  with:
    path: ~/.npm
    key: ${{ runner.os }}-npm
```

手元では再現するのに CI では通る、という状態だった。

## わかったこと

しばらくこの形で運用してみる。
