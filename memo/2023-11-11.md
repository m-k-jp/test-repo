---
title: Viteの落とし穴
tags: [計測, TypeScript]
---

三通り試して最後の形に落ち着いた。

## 背景

設定を一つ変えるだけで挙動が変わる。ドキュメントには書かれているが、条件が一つある。

## やったこと

```ts
export function toSlug(input: string): string {
  return input.trim().toLowerCase().replace(/\s+/g, '-');
}
```

数字で見ると差は明らかだった。

## わかったこと

次に同じ場面が来たら最初からこうする。
