---
title: ビルドを捨てる決断
tags: [フロントエンド, インフラ, 運用]
---

二度目なので今度こそ記録に残す。

## 背景

設定を一つ変えるだけで挙動が変わる。数字で見ると差は明らかだった。

## やったこと

```css
:root { --gap: clamp(0.5rem, 2vw, 1.5rem); }
.grid { display: grid; gap: var(--gap); }
```

手元では再現するのに CI では通る、という状態だった。

## わかったこと

次に同じ場面が来たら最初からこうする。
