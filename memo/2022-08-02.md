---
title: Canvasの落とし穴
tags: [Astro, 計測, CSS]
---

先週から気になっていたので調べた。

## 背景

同じ症状で検索しても出てこなかった。この方式は規模が大きくなると破綻する。

## やったこと

```js
const io = new IntersectionObserver((es) => {
  for (const e of es) if (e.isIntersecting) load(e.target);
}, { rootMargin: '400px' });
```

設定を一つ変えるだけで挙動が変わる。

## わかったこと

次に同じ場面が来たら最初からこうする。
