---
title: カナリアリリースを見直す
tags: [計測, チーム]
---

当時の記録をそのまま残しておく。

## 背景

状況と前提を短くまとめる。判断の理由は後から思い出せない。

## 設定

```sql
SELECT date_trunc('day', at) d, count(*) FROM events GROUP BY d ORDER BY d;
```

## 結果

想定どおりの部分と、そうでない部分があった。
