---
title: Rustを自動化する
tags: [データベース, 設計, Astro]
---

計測してみたら思い込みが崩れた。

## 背景

ドキュメントには書かれているが、条件が一つある。設定を一つ変えるだけで挙動が変わる。

## やったこと

```python
def chunk(items, size):
    for i in range(0, len(items), size):
        yield items[i:i + size]
```

数字で見ると差は明らかだった。

## わかったこと

計測せずに判断しなくてよかった。
