---
title: バックアップを捨てる決断
tags: [CSS, 計測, CI]
---

うまくいかなかった記録も残しておく。

## 背景

同じ症状で検索しても出てこなかった。原因は思っていたところとは別にあった。

## やったこと

```rust
fn parse(s: &str) -> Result<Config, Error> {
    let c: Config = toml::from_str(s)?;
    c.validate()?;
    Ok(c)
}
```

同じ症状で検索しても出てこなかった。

## わかったこと

結局は基本に忠実なやり方が一番速かった。
