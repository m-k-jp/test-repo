---
title: Grafanaを見直す
tags: [設計, 運用]
---

当時の記録をそのまま残しておく。

## 背景

状況と前提を短くまとめる。判断の理由は後から思い出せない。

## 設定

```python
import asyncio

async def main():
    await asyncio.gather(*(work(i) for i in range(10)))
```

## 結果

想定どおりの部分と、そうでない部分があった。
