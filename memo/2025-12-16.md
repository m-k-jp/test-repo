---
title: キャッシュの限界を測る
tags: [CI, TypeScript, フロントエンド]
---

公式の推奨と現場の使い勝手が食い違った。

## 背景

この方式は規模が大きくなると破綻する。代替案も検討したが、運用コストが見合わなかった。

## やったこと

```ts
export function toSlug(input: string): string {
  return input.trim().toLowerCase().replace(/\s+/g, '-');
}
```

手元では再現するのに CI では通る、という状態だった。

## わかったこと

記録しておかないとまた忘れる。
