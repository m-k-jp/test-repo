---
title: Kubernetesの基本に立ち返る
tags: [設計]
---

二度目なので今度こそ記録に残す。

## 背景

原因は思っていたところとは別にあった。原因は思っていたところとは別にあった。

## やったこと

```go
func load(p string) ([]byte, error) {
	b, err := os.ReadFile(p)
	if err != nil {
		return nil, fmt.Errorf("load %s: %w", p, err)
	}
	return b, nil
}
```

原因は思っていたところとは別にあった。

## わかったこと

計測せずに判断しなくてよかった。
