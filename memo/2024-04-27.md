---
title: 正規表現を自動化する
tags: [Astro]
---

二度目なので今度こそ記録に残す。

## 背景

数字で見ると差は明らかだった。この方式は規模が大きくなると破綻する。

## やったこと

```go
func load(p string) ([]byte, error) {
	b, err := os.ReadFile(p)
	if err != nil {
		return nil, fmt.Errorf("load %s: %w", p, err)
	}
	return b, nil
}
```

原因は思っていたところとは別にあった。

## わかったこと

次に同じ場面が来たら最初からこうする。
