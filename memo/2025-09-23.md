---
title: リファクタリングの運用メモ
tags: [インフラ, 設計, TypeScript]
---

小さな話だが積み重なると効いてくる。

## 背景

設定を一つ変えるだけで挙動が変わる。数字で見ると差は明らかだった。

## やったこと

```tsx
export function Badge({ label }: Props) {
  return <span class="badge">{label}</span>;
}
```

同じ症状で検索しても出てこなかった。

## わかったこと

次に同じ場面が来たら最初からこうする。
