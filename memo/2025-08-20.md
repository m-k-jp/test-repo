---
title: 監視の限界を測る
tags: [Astro]
---

小さな話だが積み重なると効いてくる。

## 背景

数字で見ると差は明らかだった。この方式は規模が大きくなると破綻する。

## やったこと

```rust
fn parse(s: &str) -> Result<Config, Error> {
    let c: Config = toml::from_str(s)?;
    c.validate()?;
    Ok(c)
}
```

数字で見ると差は明らかだった。

## わかったこと

結局は基本に忠実なやり方が一番速かった。
