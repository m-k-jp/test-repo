---
title: 並行処理の限界を測る
tags: [CSS, 設計, Astro]
---

三通り試して最後の形に落ち着いた。

## 背景

この方式は規模が大きくなると破綻する。原因は思っていたところとは別にあった。

## やったこと

```css
:root { --gap: clamp(0.5rem, 2vw, 1.5rem); }
.grid { display: grid; gap: var(--gap); }
```

手元では再現するのに CI では通る、という状態だった。

## わかったこと

結局は基本に忠実なやり方が一番速かった。
