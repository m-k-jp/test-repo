---
title: バックアップの勘所
tags: [運用]
---

うまくいかなかった記録も残しておく。

## 背景

設定を一つ変えるだけで挙動が変わる。原因は思っていたところとは別にあった。

## やったこと

```go
func load(p string) ([]byte, error) {
	b, err := os.ReadFile(p)
	if err != nil {
		return nil, fmt.Errorf("load %s: %w", p, err)
	}
	return b, nil
}
```

原因は思っていたところとは別にあった。

## わかったこと

次に同じ場面が来たら最初からこうする。
