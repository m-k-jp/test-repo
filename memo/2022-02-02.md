---
title: WebAssemblyを自動化する
tags: [計測, データベース, 運用]
---

小さな話だが積み重なると効いてくる。

## 背景

同じ症状で検索しても出てこなかった。数字で見ると差は明らかだった。

## やったこと

```js
const io = new IntersectionObserver((es) => {
  for (const e of es) if (e.isIntersecting) load(e.target);
}, { rootMargin: '400px' });
```

手元では再現するのに CI では通る、という状態だった。

## わかったこと

結局は基本に忠実なやり方が一番速かった。
