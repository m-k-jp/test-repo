---
title: Rustを自動化する
tags: [CI, インフラ]
---

先週から気になっていたので調べた。

## 背景

原因は思っていたところとは別にあった。手元では再現するのに CI では通る、という状態だった。

## やったこと

```js
const io = new IntersectionObserver((es) => {
  for (const e of es) if (e.isIntersecting) load(e.target);
}, { rootMargin: '400px' });
```

原因は思っていたところとは別にあった。

## わかったこと

計測せずに判断しなくてよかった。
