---
title: Kubernetesを自動化する
tags: [Astro]
---

先週から気になっていたので調べた。

## 背景

数字で見ると差は明らかだった。設定を一つ変えるだけで挙動が変わる。

## やったこと

```python
def chunk(items, size):
    for i in range(0, len(items), size):
        yield items[i:i + size]
```

ドキュメントには書かれているが、条件が一つある。

## わかったこと

しばらくこの形で運用してみる。
