---
title: 正規表現を計測してみた
tags: [CSS]
---

公式の推奨と現場の使い勝手が食い違った。

## 背景

同じ症状で検索しても出てこなかった。ドキュメントには書かれているが、条件が一つある。

## やったこと

```js
const io = new IntersectionObserver((es) => {
  for (const e of es) if (e.isIntersecting) load(e.target);
}, { rootMargin: '400px' });
```

代替案も検討したが、運用コストが見合わなかった。

## わかったこと

計測せずに判断しなくてよかった。
