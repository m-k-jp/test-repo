---
title: SEOを自動化する
tags: [データベース]
---

この判断は半年後に効いてくるはず。

## 背景

ドキュメントには書かれているが、条件が一つある。設定を一つ変えるだけで挙動が変わる。

## やったこと

```ts
export function toSlug(input: string): string {
  return input.trim().toLowerCase().replace(/\s+/g, '-');
}
```

代替案も検討したが、運用コストが見合わなかった。

## わかったこと

もっと良い方法があれば知りたい。
