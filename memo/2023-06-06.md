---
title: GitHub Actionsの基本に立ち返る
tags: [設計, フロントエンド, 計測]
---

公式の推奨と現場の使い勝手が食い違った。

## 背景

数字で見ると差は明らかだった。数字で見ると差は明らかだった。

## やったこと

```rust
fn parse(s: &str) -> Result<Config, Error> {
    let c: Config = toml::from_str(s)?;
    c.validate()?;
    Ok(c)
}
```

設定を一つ変えるだけで挙動が変わる。

## わかったこと

記録しておかないとまた忘れる。
