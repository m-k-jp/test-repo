---
title: Postgresで失敗した
tags: [データベース, TypeScript, CSS]
---

先週から気になっていたので調べた。

## 背景

数字で見ると差は明らかだった。ドキュメントには書かれているが、条件が一つある。

## やったこと

```js
const io = new IntersectionObserver((es) => {
  for (const e of es) if (e.isIntersecting) load(e.target);
}, { rootMargin: '400px' });
```

ドキュメントには書かれているが、条件が一つある。

## わかったこと

結局は基本に忠実なやり方が一番速かった。
