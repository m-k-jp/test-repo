---
title: 監視の限界を測る
tags: [運用, CI, インフラ]
---

先週から気になっていたので調べた。

## 背景

手元では再現するのに CI では通る、という状態だった。数字で見ると差は明らかだった。

## やったこと

```yaml
- name: Cache
  uses: actions/cache@v4
  with:
    path: ~/.npm
    key: ${{ runner.os }}-npm
```

同じ症状で検索しても出てこなかった。

## わかったこと

記録しておかないとまた忘れる。
