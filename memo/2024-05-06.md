---
title: マイグレーションを1年運用した記録
tags: [運用, 計測, 設計]
---

先週から気になっていたので調べた。

## 背景

代替案も検討したが、運用コストが見合わなかった。数字で見ると差は明らかだった。

## やったこと

```js
const io = new IntersectionObserver((es) => {
  for (const e of es) if (e.isIntersecting) load(e.target);
}, { rootMargin: '400px' });
```

原因は思っていたところとは別にあった。

## わかったこと

結局は基本に忠実なやり方が一番速かった。
