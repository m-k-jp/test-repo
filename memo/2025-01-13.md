---
title: 並行処理を見直した
tags: [CSS, 設計, フロントエンド]
---

計測してみたら思い込みが崩れた。

## 背景

原因は思っていたところとは別にあった。この方式は規模が大きくなると破綻する。

## やったこと

```yaml
- name: Cache
  uses: actions/cache@v4
  with:
    path: ~/.npm
    key: ${{ runner.os }}-npm
```

同じ症状で検索しても出てこなかった。

## わかったこと

結局は基本に忠実なやり方が一番速かった。
