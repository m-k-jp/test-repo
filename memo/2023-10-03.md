---
title: CSSの運用メモ
tags: [Astro, データベース, 設計]
---

結論から書くと、想定と違った。

## 背景

手元では再現するのに CI では通る、という状態だった。数字で見ると差は明らかだった。

## やったこと

```rust
fn parse(s: &str) -> Result<Config, Error> {
    let c: Config = toml::from_str(s)?;
    c.validate()?;
    Ok(c)
}
```

設定を一つ変えるだけで挙動が変わる。

## わかったこと

しばらくこの形で運用してみる。
