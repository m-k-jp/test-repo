---
title: Nginxを見直した
tags: [設計]
---

三通り試して最後の形に落ち着いた。

## 背景

設定を一つ変えるだけで挙動が変わる。設定を一つ変えるだけで挙動が変わる。

## やったこと

```js
const io = new IntersectionObserver((es) => {
  for (const e of es) if (e.isIntersecting) load(e.target);
}, { rootMargin: '400px' });
```

原因は思っていたところとは別にあった。

## わかったこと

記録しておかないとまた忘れる。
