---
title: フォントを捨てる決断
tags: [データベース, Astro, TypeScript]
---

計測してみたら思い込みが崩れた。

## 背景

原因は思っていたところとは別にあった。ドキュメントには書かれているが、条件が一つある。

## やったこと

```sh
#!/bin/sh
set -eu
find dist -name '*.js' -exec gzip -kf {} +
du -sh dist | cut -f1
```

手元では再現するのに CI では通る、という状態だった。

## わかったこと

計測せずに判断しなくてよかった。
