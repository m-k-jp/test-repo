---
title: 正規表現の基本に立ち返る
tags: [CI, TypeScript, 運用]
---

先週から気になっていたので調べた。

## 背景

設定を一つ変えるだけで挙動が変わる。代替案も検討したが、運用コストが見合わなかった。

## やったこと

```go
func load(p string) ([]byte, error) {
	b, err := os.ReadFile(p)
	if err != nil {
		return nil, fmt.Errorf("load %s: %w", p, err)
	}
	return b, nil
}
```

代替案も検討したが、運用コストが見合わなかった。

## わかったこと

次に同じ場面が来たら最初からこうする。
