---
title: Kubernetesの使い分け
tags: [計測, インフラ, データベース]
---

最初は複雑にしすぎていた。削ったら短くなった。

## 背景

同じ症状で検索しても出てこなかった。代替案も検討したが、運用コストが見合わなかった。

## やったこと

```ts
export function toSlug(input: string): string {
  return input.trim().toLowerCase().replace(/\s+/g, '-');
}
```

この方式は規模が大きくなると破綻する。

## わかったこと

しばらくこの形で運用してみる。
