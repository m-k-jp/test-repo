---
title: Goを自動化する
tags: [TypeScript, インフラ]
---

二度目なので今度こそ記録に残す。

## 背景

数字で見ると差は明らかだった。ドキュメントには書かれているが、条件が一つある。

## やったこと

```yaml
- name: Cache
  uses: actions/cache@v4
  with:
    path: ~/.npm
    key: ${{ runner.os }}-npm
```

この方式は規模が大きくなると破綻する。

## わかったこと

記録しておかないとまた忘れる。
