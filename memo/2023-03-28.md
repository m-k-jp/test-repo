---
title: Rustを計測してみた
tags: [TypeScript]
---

先週から気になっていたので調べた。

## 背景

手元では再現するのに CI では通る、という状態だった。ドキュメントには書かれているが、条件が一つある。

## やったこと

```yaml
- name: Cache
  uses: actions/cache@v4
  with:
    path: ~/.npm
    key: ${{ runner.os }}-npm
```

原因は思っていたところとは別にあった。

## わかったこと

結局は基本に忠実なやり方が一番速かった。
