---
title: Service Workerを短くする
tags: [フロントエンド]
---

二度目なので今度こそ記録に残す。

## 背景

この方式は規模が大きくなると破綻する。手元では再現するのに CI では通る、という状態だった。

## やったこと

```python
def chunk(items, size):
    for i in range(0, len(items), size):
        yield items[i:i + size]
```

同じ症状で検索しても出てこなかった。

## わかったこと

結局は基本に忠実なやり方が一番速かった。
