---
title: GitHub Actionsで失敗した
tags: [インフラ, Astro, データベース]
---

計測してみたら思い込みが崩れた。

## 背景

同じ症状で検索しても出てこなかった。数字で見ると差は明らかだった。

## やったこと

```ts
export function toSlug(input: string): string {
  return input.trim().toLowerCase().replace(/\s+/g, '-');
}
```

設定を一つ変えるだけで挙動が変わる。

## わかったこと

記録しておかないとまた忘れる。
