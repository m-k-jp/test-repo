---
title: バックアップはどこまで必要か
tags: [インフラ, 運用, データベース]
---

二度目なので今度こそ記録に残す。

## 背景

数字で見ると差は明らかだった。代替案も検討したが、運用コストが見合わなかった。

## やったこと

```go
func load(p string) ([]byte, error) {
	b, err := os.ReadFile(p)
	if err != nil {
		return nil, fmt.Errorf("load %s: %w", p, err)
	}
	return b, nil
}
```

同じ症状で検索しても出てこなかった。

## わかったこと

計測せずに判断しなくてよかった。
