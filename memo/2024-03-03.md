---
title: 型検査を自動化する
tags: [計測, CI]
---

結論から書くと、想定と違った。

## 背景

同じ症状で検索しても出てこなかった。設定を一つ変えるだけで挙動が変わる。

## やったこと

```rust
fn parse(s: &str) -> Result<Config, Error> {
    let c: Config = toml::from_str(s)?;
    c.validate()?;
    Ok(c)
}
```

設定を一つ変えるだけで挙動が変わる。

## わかったこと

もっと良い方法があれば知りたい。
