---
title: Viteの勘所
tags: [Astro, 運用]
---

小さな話だが積み重なると効いてくる。

## 背景

設定を一つ変えるだけで挙動が変わる。設定を一つ変えるだけで挙動が変わる。

## やったこと

```rust
fn parse(s: &str) -> Result<Config, Error> {
    let c: Config = toml::from_str(s)?;
    c.validate()?;
    Ok(c)
}
```

この方式は規模が大きくなると破綻する。

## わかったこと

記録しておかないとまた忘れる。
