---
title: セマンティックバージョニングの使い分け
tags: [CSS, TypeScript]
---

三通り試して最後の形に落ち着いた。

## 背景

代替案も検討したが、運用コストが見合わなかった。手元では再現するのに CI では通る、という状態だった。

## やったこと

```tsx
export function Badge({ label }: Props) {
  return <span class="badge">{label}</span>;
}
```

ドキュメントには書かれているが、条件が一つある。

## わかったこと

記録しておかないとまた忘れる。
