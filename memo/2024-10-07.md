---
title: セマンティックバージョニングを見直した
tags: [フロントエンド, CSS]
---

二度目なので今度こそ記録に残す。

## 背景

数字で見ると差は明らかだった。ドキュメントには書かれているが、条件が一つある。

## やったこと

```js
const io = new IntersectionObserver((es) => {
  for (const e of es) if (e.isIntersecting) load(e.target);
}, { rootMargin: '400px' });
```

手元では再現するのに CI では通る、という状態だった。

## わかったこと

しばらくこの形で運用してみる。
